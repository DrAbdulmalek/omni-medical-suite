# STATUS: TEMPLATE ONLY — NOT EXECUTED
# PHASE: V-2
# TESTED: NO
# ⚠️ هذا الملف (باسم .env الحقيقي) لا يُرفع إلى Git أبدًا — .gitignore يغطيه.
# ⚠️ PHI غير مسموح على VPS عام — راجع docs/future/vps/PRIVACY_COMPLIANCE.md
# ⚠️ القيم أدناه PLACEHOLDERS تُولَّد وقت التنفيذ فقط (openssl rand -hex 32)

FLASK_SECRET_KEY=<generate-with-openssl-rand-hex-32>
ATR_SECRET_KEY=<generate-with-openssl-rand-hex-32>
ATR_API_TOKEN=<generate-with-openssl-rand-hex-32>
# ATR_ALLOW_REMOTE=1        # فك التعليق فقط مع nginx/ufw مضبوطين (fail-closed بدونهما)
# ATR_CORS_ORIGINS=https://<DOMAIN>
AHW_SAMPLE_ID=S001
AHW_DB_PATH=/home/ubuntu/omni-medical-suite/output/S001/corrections.db
AHW_MAX_UPLOAD_MB=100
BACKUP_REPO=<your-private-datasets-repo-url>
BACKUP_RETENTION_DAYS=30
