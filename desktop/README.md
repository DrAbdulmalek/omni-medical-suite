# ⚠️ DEPRECATED — Legacy Desktop GUI

This directory contains a legacy standalone desktop GUI script.

The canonical desktop package is located at:
- **`packages/desktop/`** — Modular Tkinter GUI with tests, multiple versions, and installation scripts.

## Migration Path

Use `packages/desktop/` for all new development. This legacy script is kept for reference only
and will be removed in a future release.
