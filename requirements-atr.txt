# =============================================================================
# requirements-atr.txt — تبعيات حزمة AHW (ATR phase) الخفيفة
# =============================================================================
# منفصل عن requirements.txt الجذري (المنظومة الكاملة) — يُستخدم من
# Dockerfile.atr ومن venv التطوير. سجل الإضافة الكامل (اسم/إصدار/رخصة/سبب)
# في docs/atr/ATR-07-FINAL-REPORT.md قسم DEPENDENCIES_ADDED.
#
# ملاحظة torch: في Docker CPU يُثبَّت من الفهرس الرسمي:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# ثم بقية الحزم أدناه (أو فك التعليق عن سطر torch إن أردت الافتراضي PyPI).
# =============================================================================

# ML core
torch>=2.1.0
transformers>=4.46.0,<5.0.0   # pin جلسة Qwen: 4.57.6 مختبَر 15/15 (هذا السطر من الجلسة الموازية)
# تنبيه مصالحة (جلسة Z.ai، py3.12/torch2.14، مُثبت): 4.48.3 يكسر تدريب TrOCR عبر
# Trainer (num_items_in_batch يرفضه ViTModel.forward — traceback موثق)، بينما
# 5.12.1 يعمل كاملًا (dry 4/4 + smoke Trainer + real-load 2.67GB) — متوافق مع
# حدّ المستودع الموثق transformers>=5.10,<5.13 في worklog TASK-02D. الدليلان
# متعارضان جزئيًا؛ يُرجى الفصل عبر CI على كلا الإصدارين قبل الدمج. راجع PROGRESS.md.
accelerate>=0.30.0

# Web (ATR-F3)
flask>=3.0.0
flask-socketio>=5.3.0
simple-websocket>=1.0.0

# PDF/صور (ATR-F4)
PyMuPDF>=1.23.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0
scipy>=1.11.0

# بيانات/تقييم
pandas>=2.0.0
openpyxl>=3.1.0
jiwer>=3.0.0

# عربية
arabic-reshaper>=3.0.0
python-bidi>=0.4.0
