# =============================================================================
# OmniMedical Suite — Gradio HITL Interface Requirements
# =============================================================================
# Primary: app/gradio_full_hitl.py (official production app, 944 lines, 10 features)
# Secondary: app/advanced_review_app.py (experimental review app — Compare/Search/Review)
# Install: pip install -r requirements/gradio.txt
# =============================================================================

-r ml.txt

# === Gradio UI ===
gradio>=4.44.0,<7.0.0
# qdrant-client>=1.9.0  # moved to [search] extra — Search tab falls back to local fuzzy without it

# === OCR Engines (primary) ===
paddleocr>=2.7.0
paddlepaddle>=2.5.0

# === HuggingFace Integration (dataset save/load) ===
huggingface_hub>=0.19.0,<1.0.0
pydantic<2.14
datasets>=2.15.0
pandas>=2.0.0

# === Deep Learning (for LLM proofreading + NER — GPU recommended) ===
# Uncomment for Jais LLM support:
# transformers>=4.36.0
# torch>=2.1.0
# peft>=0.7.0
# accelerate>=0.25.0

# === Optional Extra OCR Engines / Search ===
# easyocr>=1.7.0
# surya-ocr>=0.4.0
# timm>=0.9.0
# sentence-transformers>=2.2.0  # enables semantic ranking in the Search tab