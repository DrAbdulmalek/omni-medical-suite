# Isolated environment pins for the omni_extraction wrapper.
#
# Install in a DEDICATED virtual environment — never merge into the
# suite's main dependency set (governance: engine isolation).
#
#   python3 -m venv .venv-xberg
#   .venv-xberg/bin/pip install -r packages/omni_extraction/requirements.txt
#
# Pinned against PyPI metadata verified 2026-09-23.
xberg==1.2.6
