# =============================================================================
# OmniFile AI Processor — Development Requirements (متطلبات التطوير)
# =============================================================================
# حزم الاختبار والتدقيق فقط — لا حاجة لها للتشغيل الإنتاجي.
# الاستخدام: pip install -r requirements-dev.txt
# =============================================================================

-r requirements-core.txt

# === Testing ===
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# === Linting & Formatting ===
ruff>=0.1.0
