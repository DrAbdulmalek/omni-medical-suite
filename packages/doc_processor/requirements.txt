# ── متطلبات معالج الوثائق الطبية v12 ──────────────────────
# الأساسية (مطلوبة)
PyQt5>=5.15.0
opencv-python-headless>=4.8.0
numpy>=1.24.0

# اختيارية — دعم PDF
# pdf2image>=1.16.0
# poppler-utils  ← تثبيت عبر: sudo pacman -S poppler

# اختيارية — OCR وكشف المكررات
# pytesseract>=0.3.10
# Pillow>=9.0.0
# imagehash>=4.3.0
# tesseract-ocr + tesseract-data-ara  ← sudo pacman -S tesseract tesseract-data-ara

# اختيارية — اختبارات
pytest>=7.0.0
