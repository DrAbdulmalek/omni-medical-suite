# ⚠️ DEPRECATED — Kubernetes Manifests

This directory is **deprecated** and kept for reference only.

The canonical Kubernetes manifests are located at the repository root:
- **`k8s/`** — Active manifests used by CI/CD (`.github/workflows/cd.yml`)

## Migration Path

If you have made changes in this directory, please port them to `k8s/` and submit a PR.
Once all manifests are synchronized, this directory will be removed.
