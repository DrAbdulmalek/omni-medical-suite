# =============================================================================
# OmniMedical Suite — Unified Requirements (Legacy)
# =============================================================================
# This file is kept for backward compatibility.
# For new installations, use the layered requirements under requirements/:
#   pip install -r requirements/dev.txt    (development + testing)
#   pip install -r requirements/api.txt     (API service only)
#   pip install -r requirements/ml.txt      (ML/CV packages only)
#   pip install -r requirements/base.txt   (core shared deps only)
#   pip install -r requirements/optional.txt (optional heavy deps)
# =============================================================================

-r requirements/api.txt
-r requirements/ml.txt
