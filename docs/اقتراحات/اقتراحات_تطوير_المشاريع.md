# ملف الاقتراحات والتطوير المستقبلي
# Suggestions & Future Development
# ====================================
# التاريخ: 2026-06-25 (محدّث)
# المصدر: مراجعتان — محادثة Gemini + مراجعة Genspark

# ══════════════════════════════════════════════════════════════════
# ✅ تم تطبيقه (مؤكّد)
# ══════════════════════════════════════════════════════════════════
# 1.  ingest_and_clean_v2.py — Pipeline بدعم العربية + Dedup + TSV
#     → medical-ocr-training-hub/src/ingestion/ingest_and_clean_v2.py
# 2.  مخطط Mermaid شامل (End-to-End Data Flow) في training-hub README
# 3.  إصلاح Gradio handwriting-ocr (multiple=True → file_count="multiple")
#     → تم الدفع إلى HuggingFace
# 4.  24 اختبار آلي للـ Pipeline (PII + Dedup + Validation + TSV + E2E)
# 5.  صقل PORTFOLIO.md — جداول أبسط، 5 قواعد واضحة، مخطط Mermaid محسّن
#     → omni-medical-suite (commit 384082c)
# 6.  تصحيح HF Space metadata لـ handwriting-ocr (title + emoji + pinned)
#     → HuggingFace push ce321fb
# 7.  إشعارات الأرشفة في 4 مستودعات مؤرشفة (جلسة سابقة)
# 8.  إزالة بيانات اعتماد صلبة من omni-medical-suite (جلسة سابقة)
# 9.  PyQt5 → PySide6 في omni-medical-suite/packages/desktop (جلسة سابقة)
# 10. CI/CD لكل مستودع نشط (جلسة سابقة)

# ══════════════════════════════════════════════════════════════════
# 🔴 إجراءات يدوية مطلوبة منك (لا يمكن تطبيقها آلياً)
# ══════════════════════════════════════════════════════════════════

# ── 1. تحويل tg-forwarder HF Space إلى Private ──────────────────
# السبب: HF API لا يدعم تغيير الخصوصية (لا PATCH ولا PUT ولا CLI)
# الإجراء:
#   1. افتح: https://huggingface.co/spaces/DrAbdulmalek/tg-forwarder/settings
#   2. غيّر Visibility → Private
#   3. اضغط Save
# الأثر: يختفي من الصفحة العامة فوراً — يزيل انطباع "No application file"

# ── 2. إخفاء/أرشفة HF Spaces المرتبطة بمستودعات مؤرشفة ──────
# HF API لا يدعم الأرشفة ولا تغيير الخصوصية
# Spaces المعنية:
#   • OmniFile-Processor       → المستودع مؤرشف على GitHub، Space لا فائدة منه
#   • medical-handwriting-ocr   → المستودع مؤرشف، يتكرر مع handwriting-ocr
#   • medical-ocr-trainer      → بدون تطبيق فعلي (Sleeping)
#   • omnifile-processor-lab   → دليل تجريبي
#   • telegram-forwarder       → يحتوي على أداة حساسة (أداة تيليجرام)
#
# الإجراء المقترح (من الموقع لكل Space):
#   أ) OmniFile-Processor → Private أو حذف (المستودع مؤرشف)
#   ب) medical-handwriting-ocr → Private (يتكرر مع handwriting-ocr النشط)
#   ج) medical-ocr-trainer → Private حتى يُفعّل بربطه بـ training-hub
#   د) omnifile-processor-lab → Private أو حذف
#   هـ) telegram-forwarder → Private (أداة حساسة)
#
# النتيجة: يبقى فقط handwriting-ocr (Primary Demo) + arabic-translation-corrector

# ── 3. توحيد أسماء HF Spaces (تغيير الاسم غير مدعوم عبر API) ──
# الأسماء الحالية مربكة:
#   medical-ocr-trainer vs handwriting-ocr vs medical-handwriting-ocr
# الإجراء (يدوي من إعدادات كل Space):
#   الأسماء المقترحة:
#     medical-ocr-trainer      → demo-medical-trainer (لو فُعّل)
#     handwriting-ocr          → demo-handwriting-ocr (Primary Demo ✅)
#     arabic-translation-corrector → demo-arabic-translation (لو فُعّل)

# ── 4. توحيد صيغة إشعارات الأرشفة في المستودعات المقروءة فقط ──
# المشكلة: المستودعات الأربعة المؤرشفة لا يمكن تعديلها (read-only)
# ولكن صياغة الإشعارات غير متسقة:
#   • medical-handwriting-ocr: "مؤرشف" + "ARCHIVED" ✅
#   • medical-ocr-postprocessor: "مؤرشف" + "ARCHIVED" ✅
#   • OmniFile_Processor: "LEGACY NOTICE" + "maintenance mode" ⚠️ مختلف
#   • medical-doc-processor: "LEGACY" + "being merged" ⚠️ مختلف
#
# الحل: لا يمكن التعديل لأنها archived (read-only)
# البديل: هذا تناقض طفيف فقط في الصياغة، لا يؤثر على الوظيفة

# ══════════════════════════════════════════════════════════════════
# 🟠 اقتراحات استراتيجية (تحتاج قرارات معمارية)
# ══════════════════════════════════════════════════════════════════

# ── 5. بوابة الاستيعاب الموحدة (Unified Ingestion Gateway) ──────
# الفكرة: تحويل IntelliFile-app إلى "عقدة طرفية" (Edge Node)
# يحتاج قرارات:
#   أ) آلية الاستشعار: Background Daemon (watchdog) أم GUI Drag & Drop؟
#   ب) خط الأمان: تشفير محلي قبل الرفع أم PHI Masking فقط؟
#   ج) إدارة الحالة: SQLite محلي لتتبع حالة الملفات؟

# ── 6. أتمتة حلقة التعلم المستمر (Automated MLOps Loop) ────────
# الربط التلقائي: HF correction → training-hub → ground-truth → benchmarks → نشر
# يحتاج:
#   • Webhook من HF عند إضافة تصحيحات
#   • GitHub Actions في training-hub
#   • Dataset حقيقي على HF: DrAbdulmalek/medical-ocr-corrections

# ── 7. التخصيص العميق لجراحة العظام (Orthopedic NER) ──────────
# يكتشف: أنواع الكسور، أسماء الغرسات، تقارير الأشعة
# يحتاج: corpus موسوم + اختيار نموذج (CAMeL-BERT / AraBERT)

# ── 8. Privacy Middleware إلزامي ───────────────────────────────
# تحويل PII scrubbing إلى Middleware لا يمكن تجاوزه في omni-medical-suite
# إضافة NER-based PII (AraBERT) بدلاً من Regex فقط

# ── 9. توحيد Core Package (@omni/core) ─────────────────────────
# إنشاء Package مشترك بين المستودعات (نصوص، PII، DB، Config)
# نشره كـ pip package داخلي أو git submodule

# ── 10. ربط bilingual-extractor بـ ai-fuel-engine ──────────────
# تدفق: محاذاة AR/EN → تنظيف → إزالة تكرار → HF Dataset
# يحتاج: استنساخ المستودعين + تصميم API + CI/CD

# ══════════════════════════════════════════════════════════════════
# 🟡 اقتراحات تقنية سريعة (يمكن تطبيقها بسرعة)
# ══════════════════════════════════════════════════════════════════

# 11. إضافة AraBERT/CAMeL-BERT كبديل لـ Regex في PII detection
#     يكتشف الأسماء العربية (محمد، فاطمة) التي لا يلتقطها Regex
#     → إضافته كطبقة في ingest_and_clean_v3.py

# 12. إنشاء Dataset حقيقي على HF: DrAbdulmalek/medical-ocr-corrections
#     حالياً Pipeline يعمل على mock data فقط

# 13. إضافة دعم إشعارات Telegram في Pipeline
#     عند اكتمال التدريب أو تراجع الدقة

# 14. إصلاح README الخاص بـ HF Space handwriting-ocr
#     المحتوى الحالي يصف OmniFile AI Processor وليس handwriting-ocr
#     يحتاج إعادة كتابة README ليتوافق مع محتوى Space الفعلي

# ══════════════════════════════════════════════════════════════════
# 📊 أولويات مقترحة
# ══════════════════════════════════════════════════════════════════
#
# فوري (يدوي — 10 دقائق):
#   • تحويل tg-forwarder → Private                         ← #1
#
# قصير (1-2 يوم):
#   • إخفاء Spaces النائمة/المكررة من HF                  ← #2
#   • إصلاح README handwriting-ocr HF Space                ← #14
#
# متوسط (1-2 أسبوع):
#   • تصميم بوابة الاستيعاب الموحدة (IntelliFile)          ← #5
#   • إنشاء Dataset حقيقي على HF                           ← #12
#   • توحيد أسماء HF Spaces                                ← #3
#
# طويل (1-3 أشهر):
#   • حلقة MLOps المؤتمتة                                  ← #6
#   • NER متخصص في جراحة العظام                            ← #7
#   • Privacy Middleware إلزامي                             ← #8
#   • توحيد Core Package                                    ← #9