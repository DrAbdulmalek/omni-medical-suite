# ROLLBACK PLAN (PLANNED)

## متى نرجع؟
- فشل نشر/تحديث كسر الخدمة ولم يُصلح خلال ساعة.
- حادث أمني (اختراق مشتبه، تسرب أسرار/بيانات).
- نموذج مُرقّى أثبت CER أسوأ من السابق (rollback آلي — V-6).
- Oracle علّق الحساب (انتقال للبديل — ALTERNATIVES).

## كيف نرجع؟ (مرتبة)
1. **خدمة**: `systemctl stop ahw-*` → `git checkout <last-good-tag>` → إعادة تشغيل → تحقق (10-verify).
2. **بيانات**: استعادة آخر backup مشفر (فك GPG → تحقق SHA256 + عدّ الصفوف → إرجاع).
3. **نموذج**: `automation/vps/04-promote-or-rollback.sh.template` يعيد `models/previous/` تلقائيًا ويوثق القرار.
4. **خادم كامل**: VM جديد من 02→08 (القوالب idempotent) + استعادة بيانات — **لذلك كل شيء قوالب + نسخ**.
5. **مغادرة Oracle**: الخطة البديلة Codespaces+Colab+GitHub (لا اعتماد على VM).

## بعد أي rollback
- توثيق: السبب/الوقت/الخطوات/النتيجة في سجل النشر.
- **الاتصال بالمالك** فورًا إن كان الحادث أمنيًا أو فقدان بيانات >24h.
- تدوير الأسرار المتأثرة دائمًا (بلا استثناء).

**Status: PLANNED · لم يُختبر أي rollback (لا يوجد نشر).**
